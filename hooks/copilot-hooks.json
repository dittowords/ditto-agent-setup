{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "bash": "cat \"${PLUGIN_ROOT}/hooks/ditto-instructions.md\"",
        "powershell": "Get-Content \"${PLUGIN_ROOT}\\hooks\\ditto-instructions.md\"",
        "timeoutSec": 5
      }
    ],
    "userPromptSubmitted": [
      {
        "type": "command",
        "bash": "echo 'Reminder: user-facing copy goes through Ditto (styleguide rules + text reuse via the ditto MCP tools).'",
        "powershell": "Write-Output 'Reminder: user-facing copy goes through Ditto (styleguide rules + text reuse via the ditto MCP tools).'",
        "timeoutSec": 5
      }
    ]
  }
}
